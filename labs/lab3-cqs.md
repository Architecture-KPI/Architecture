# Лабораторна робота 3: CQS (Command-Query Separation)

## Мета

Розділити операції запису (команди) та читання (запити) на рівні Application Layer. Студенти бачать, що читання і запис мають різні вимоги до коду, тестування та оптимізації.

## Теоретичні матеріали

Перед виконанням лабораторної опрацюйте:
- [CQS та CQRS](../docs/cqs.md)
- [DTO (Data Transfer Object)](../docs/dto.md)
- [Repository Pattern](../docs/repository.md) (розділ про Read Repository та CQS)

## Завдання

### 1. Виділити команди (Commands) та їхні обробники (Command Handlers)

Команда — об'єкт-намір, що описує дію, яка змінює стан системи. Кожна команда обробляється одним `CommandHandler`.

Студент сам визначає набір команд, виходячи з юзкейсів із лабораторної 1. Кожна операція запису (створення, оновлення, видалення, зміна статусу) — це окрема команда.

Вимоги до команд:
- Команда — простий об'єкт з даними (DTO), **без логіки**
- Handler проходить через домен, валідує інваріанти
- Команда **не повертає дані** (допустимий виняток — ID створеної сутності)

### 2. Виділити запити (Queries) та їхні обробники (Query Handlers)

Запит — об'єкт, що описує, які дані потрібно отримати. Не змінює стан системи.

Студент сам визначає набір запитів, виходячи з юзкейсів із лабораторної 1. Кожна операція читання (отримання списку, пошук, фільтрація, отримання деталей) — це окремий запит.

Вимоги до запитів:
- Запити можуть читати **напряму з БД**, минаючи доменний шар
- Запити повертають **Read Models** (DTO), оптимізовані під потреби клієнта — не доменні моделі
- Запити **не мутують стан**

### 3. Реалізувати Read Models

Read Models — окремі моделі для читання, які відрізняються від доменних моделей:

| Тип моделі | Призначення |
|------------|-------------|
| Доменна модель | Бізнес-логіка, інваріанти, поведінка |
| Read Model | Передача даних клієнту, оптимізована під конкретний запит |

Read Model може містити денормалізовані дані з кількох сутностей (наприклад, ім'я пов'язаного об'єкта поруч з основною інформацією), щоб клієнту не потрібно робити додаткові запити. Студент сам визначає, які Read Models потрібні, виходячи зі своїх запитів.

### 4. Рефакторинг Presentation Layer

Контролери стають «тонкими» — їхня єдина задача: маппінг HTTP-запиту в Command або Query та передача відповідному Handler.

До (лаба 2):
```
Controller → UseCase / ApplicationService → Domain → Repository
```

Після (лаба 3):
```
Controller → Command  → CommandHandler → Domain → Repository
Controller → Query    → QueryHandler   → Repository (read model)
```

Вимоги:
- Контролери **не містять логіки** — лише маппінг HTTP → Command / Query
- Один контролер може створювати і команди, і запити
- Контролер не знає деталей реалізації Handler'а

### 5. Оновити діаграму архітектури

Оновити `docs/architecture.md` з лабораторної 2, щоб діаграма відображала розділення на Command та Query потоки:
- Command path: Controller → Command → CommandHandler → Domain → Repository
- Query path: Controller → Query → QueryHandler → Read Repository → Read Model

### 6. Оновити структуру проєкту

Структура Application Layer змінюється — з'являються окремі папки для команд та запитів:

```
src/
├── presentation/
│   ├── controllers/
│   └── dto/                  # HTTP request/response DTO
├── application/
│   ├── commands/             # Command + CommandHandler пари
│   ├── queries/              # Query + QueryHandler пари
│   └── read_models/          # DTO для Query responses
├── domain/
│   ├── entities/
│   ├── aggregates/
│   ├── value-objects/
│   ├── factories/
│   └── repositories/
└── infrastructure/
    ├── repositories/
    ├── orm_models/
    └── mappers/
```

### 7. Тестування

Команди та запити тестуються **по-різному**:

**Тести команд** (поведінкові, given-when-then):
- **Given**: початковий стан системи (існуючий користувач, ресурс, можливо — існуюче бронювання)
- **When**: виконуємо команду
- **Then**: перевіряємо побічний ефект (об'єкт створено/змінено в репозиторії, статус оновлено, тощо)

**Тести запитів** (fixture → query → assert):
- Підготували тестові дані (fixture)
- Виконали запит
- Перевірили, що відповідь містить очікувані дані у правильному форматі (Read Model)

Студент має **проаналізувати**: що стало простіше тестувати порівняно з лабою 2, що складніше?

### 8. Написати аналіз

Порівняльний аналіз (1-2 сторінки), що відповідає на питання:

- Що змінилося в структурі проєкту порівняно з лабораторною 2?
- Переваги CQS: чим допомогло розділення?
- Недоліки CQS: що ускладнилося?
- Чим підхід з Command / Query Handler відрізняється від Service, який робить все сам?
- Як CQS впливає на розширюваність? (додати нову команду vs змінити існуючий сервіс)
- Як би ви оптимізували Query-частину окремо від Command-частини?

Розмістити в `docs/analysis/lab3.md`.

## Вимоги до процесу

- **Git Flow**: продовжуємо процес з лабораторної 1 (гілки `main` / `dev`, PR з code review та approve, conventional commits)
- **CI**: pipeline зелений, тести проходять
- **Тег релізу**: `lab-3.0` на коміті в `main`

## Що здати

| Артефакт | Опис |
|----------|------|
| Рефакторений проєкт | Commands, Queries, Handlers, Read Models |
| Структура проєкту | Окремі папки для commands / queries / read_models |
| Діаграма архітектури | Оновлена `docs/architecture.md` з command/query потоками |
| Тести | Команди (given-when-then) + запити (fixture-query-assert) |
| Аналіз | `docs/analysis/lab3.md` — порівняння з лабою 2 |
| Git Flow | PR з review, тег `lab-3.0` на `main` |

## Критерії оцінювання

| Критерій | На що звернути увагу |
|----------|---------------------|
| Команди | Не повертають дані (або лише ID), проходять через домен |
| Запити | Не мутують стан, повертають Read Models |
| Read Models | Відрізняються від доменних моделей, оптимізовані для клієнта |
| Handlers | Маленькі, з однією відповідальністю, делегують домену |
| Контролери | Містять лише маппінг HTTP → Command / Query |
| Діаграма | Оновлена діаграма з лаби 2, видно два шляхи: write через домен, read напряму |
| Тестування | Різний підхід до тестування команд і запитів |
| Аналіз | Конкретне порівняння з лабою 2, плюси та мінуси CQS |
